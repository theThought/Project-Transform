import { Meta } from '@storybook/blocks';

<Meta title="Organisms" />

# Organisms
## Dropdowns and comboxes
- Consider refactoring the CSS for `<input class="a-input-dropdown" />` as it is only styled correctly if it's a descendent of `"o-dropdown"`?
- Similarly, refactor `<input class="a-input-combobox" />`?
